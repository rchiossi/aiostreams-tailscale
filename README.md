# AIOStream Docker image with Tailscale dependencies

The Docker image from https://github.com/Viren070/AIOStreams uses a stripped down base distro that does not have the required dependencies to get tailscale running with the Unraid integration. This repo is a wrapper to enable that integration.
